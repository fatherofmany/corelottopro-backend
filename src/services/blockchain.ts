// Placeholder for contract interaction utilities.
// You can add ethers or viem here if you need on-chain reads/writes from the backend.
// Example:
// import { createPublicClient, http } from 'viem'
// export const client = createPublicClient({ chain: ..., transport: http(process.env.RPC_URL) })
